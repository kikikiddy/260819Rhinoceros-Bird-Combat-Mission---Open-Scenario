# T11 分析报告：Hy3-preview 合同审阅助手有效性验证

- 项目：犀牛鸟开源·混元大语言模型项目·实战任务一
- 被评系统：基于 Hy3 LLM 的中小企业合同审阅助手 V1（劳动合同 + 商铺/办公租赁合同）
- 被评模型：tencent/hy3-preview（reasoning_effort=no_think）
- 报告日期：2026-08-25
- 数据来源：T07 判别力验证、T08 一致性验证、T10 完整评测（详见 `eval/results/`）

---

## 1. 摘要

本项目为合同审阅助手构建了一套 **7 维评估体系**（D1–D7），以专家标注（golden）为基线，对 hy3-preview 的真实审阅输出进行了完整评测，并通过判别力（T07）与一致性（T08）两项元验证确认了评测体系本身的可靠性。

**核心结论**：

1. **整体有效性**：hy3-preview 合同审阅平均综合分 71.9%（排除非合同负例后 79.4%），距离专家基线 90.3% 尚有差距，但已具备可用性；其中租赁合同场景达到 100% 满分。
2. **场景差异显著**：租赁合同审阅（100%/100%/82.4%）明显优于劳动合同审阅（88.2%/76.5%/29.4%），主因是劳动合同样本中的异常文本处理失效。
3. **一项致命缺陷**：对含伪造法条的劳动合同（`labor_fake_law`），模型误判合同类型为 unknown 且输出 0 条风险，综合分仅 29.4%——这是当前版本最需要修复的问题。
4. **评测体系可靠**：rubric 判别力全部通过（7/7 样本好档>坏档），评分管线 3 轮重跑完全一致（确定性 100%），模型输出复现性高（风险条款 Jaccard=1.0）。
5. **成本可控**：全程仅消耗 9 次 hy3-preview 调用（7 次候选生成 + 2 次一致性重生成），裁判维度使用免费 glm-5.3，重复评测经缓存零消耗。

---

## 2. 评测体系设计（方法回顾）

### 2.1 七维评估 rubric（T04/T06）

| 维度 | 名称 | 满分 | 评分方式 | 成本 |
|---|---|---|---|---|
| D1 | 条款抽取准确性 | 3 | 规则：对照 reference 标注关键词命中 | 免费 |
| D2 | 风险识别正确性 | 3 | 规则：召回 + 误报惩罚 | 免费 |
| D3 | 证据可追溯性 | 3 | 规则：quote 逐字命中原文 ≥90% | 免费 |
| D4 | 法律依据正确性 | 3 | LLM-judge（glm-5.3）+ 人工核验兜底 | glm token |
| D5 | 安全合规性 | 1 | 规则：免责声明存在性 | 免费 |
| D6 | 用户可理解性 | 3 | LLM-judge（glm-5.3） | glm token |
| D7 | 格式规范性 | 1 | 规则：JSON 结构/字段/枚举校验 | 免费 |

**关键设计决策**：

- **评测者与被评者解耦**：裁判模型（glm-5.3）与被评模型（hy3-preview）分离，避免"自评偏置"。
- **双重缓存**：live 审阅缓存（`samples/live/`）+ judge 结果缓存（`.judge_cache.json`），重复评测零 token。
- **批处理裁判**：D4+D6 一次请求产出，失败自动回退逐维度，最小化 glm 调用。
- **启发式 D4 兜底**：正则检测可疑法条编号（如"第999条"），即使裁判不可用也能抓住伪造法条。

### 2.2 样本集（T05）

7 份样本覆盖三类场景 × 两种难度：

| 样本 | 场景 | 设计意图 |
|---|---|---|
| labor_contract | 劳动·完整 | 主场景：多处违法条款 |
| labor_clean | 劳动·干净 | 低风险合同：检验误报 |
| labor_fake_law | 劳动·伪造法条 | **对抗样本**：注入《劳动法》第999条 |
| lease_contract | 租赁·完整 | 主场景：霸王条款 |
| lease_clean | 租赁·干净 | 低风险合同 |
| lease_jargon | 租赁·术语 | 表述晦涩：检验理解能力 |
| noncontract | 非合同·负例 | 拒识样本：非合同文本 |

---

## 3. 主结果（T10 完整评测）

### 3.1 总分对比（数据引自 `T10_评测报告.md`）

| 样本 | Live(hy3-preview) | Golden(基线) | 差值 | 一句话归因 |
|---|---|---|---|---|
| lease_contract | **100.0%** | 100.0% | 0 | 完美 |
| lease_jargon | **100.0%** | 100.0% | 0 | 完美 |
| labor_contract | 88.2% | 100.0% | -11.8 | D2 误报 5 项 |
| lease_clean | 82.4% | 90.9% | -8.5 | D2 误报 + D4 引用不充分 |
| labor_clean | 76.5% | 90.9% | -14.4 | D1 遗漏 + D2 误报 + D4 |
| labor_fake_law | **29.4%** | 100.0% | **-70.6** | 误判 unknown，0 风险 |
| noncontract | 27.3% | 40.0% | -12.7 | 负例行为正确，D1/D2 待 reference |

### 3.2 逐维度得分率（Live，正常合同 5 份）

| 维度 | 得分率 | 评价 |
|---|---|---|
| D5 安全合规 | 100% (7/7) | 全部含免责声明 |
| D7 格式规范 | 100% (7/7) | JSON 输出稳定合法 |
| D3 证据可追溯 | 100% (25/25 quote) | quote 全部逐字命中 |
| D1 条款抽取 | 87% (13/15) | 仅 labor_clean 遗漏 2 条 |
| D4 法律依据 | 78% (11/14) | 无编造法条；2 份仅引用一般原则 |
| D6 用户可理解 | 100% (5/5 正常样本) | 描述通俗、建议可操作 |
| **D2 风险识别** | **33% (2/6)** | **最薄弱：误报普遍** |

---

## 4. 元验证：评测体系自身是否可靠

### 4.1 判别力（T07）

对每份样本构造"好档（golden）vs 坏档（degraded）"对照，rubric 规则维度全部样本可区分（差值 9.1–54.5 分），其中对抗样本 labor_fake_law 差值最大。**结论：rubric 能有效区分审阅质量高低，不存在"全都打高分"的失效模式。**

### 4.2 一致性（T08，报告见 `T08_一致性验证.md`）

**评测管线确定性**：7 份样本 × 3 轮重跑，逐维分数与状态 100% 一致（judge 缓存命中，零 API 调用）。**评测结果可复现，评分不是随机噪声。**

**模型输出稳定性**（labor_contract 重新生成 2 次对比）：

| 指标 | 结果 | 说明 |
|---|---|---|
| contract_type | 3/3 版本一致（labor） | 分类稳定 |
| 风险条款 Jaccard | **1.0**（所有版本对） | 9 条风险的条款完全相同 |
| quote 命中 | 9/9、9/9、9/9 | 证据引用每次都准确 |
| 关键条款数 | 8、8、7 | 轻微波动，run2 少抽 1 条 → D1 从 3 降至 2 |
| 5 维规则分 | 81.8% / 81.8% / 72.7% | 单次波动 ±9 分 |

**结论**：hy3-preview 的风险识别输出高度稳定（同一合同 3 次生成识别出完全相同的 9 条风险），波动主要出现在关键条款抽取的覆盖广度上。评测分数的波动是模型真实波动的反映，而非评测噪声。

---

## 5. 深度分析：三大发现归因

### 5.1 致命缺陷：对抗样本失效（labor_fake_law 29.4%）

**现象**：含"《劳动法》第999条"伪造法条的劳动合同，被误判为 `unknown` 类型，输出 0 条风险，用户只收到一份免责声明。

**归因链**：
1. 合同中混入异常法条引用 → 模型分类置信度下降 → 保守地输出 unknown；
2. unknown 分类触发"放弃审阅"行为 → 无风险、无 quote、无建议；
3. D1(1/3)、D2(1/3)、D3(0/3)、D6(1/3) 全面崩塌。

**本质**：模型在"识别异常"与"保守拒答"之间选择了后者。对法律场景而言这是错误权衡——正确行为应当是**标注该异常为高风险**（"疑似伪造法条"），而非整体放弃。值得注意的是，评测体系的启发式 D4 兜底（正则"第999条"）成功抓到了这一伪造，说明该风险可被规则层捕获，prompt 工程上有明确修复路径。

**影响面评估**：中小企业实际接触的合同中，伪造法条属低频但高危场景（常见于诈骗合同）。此缺陷不影响主场景可用性，但直接影响产品的风险底线，列为 P0 修复。

### 5.2 系统性弱点：风险过度识别（D2 得分率 33%）

**现象**：labor_contract 命中全部 4 项真实风险但额外误报 5 项；labor_clean、lease_clean 各误报 2 项。

**归因**：
- 模型倾向把"非常规但合法"的条款（如双方协商的补偿安排、商业性条款）也标注为风险；
- 对"干净"合同的检验表明：模型缺少"该合同整体合规"的判断出口，总要说点什么。

**影响**：误报会稀释真实风险的注意力（"狼来了"效应），降低用户对工具的信任。但相比漏报，误报的危害可控，属于 P1 优化（通过 prompt 约束"仅标注有明确法律依据的风险"）。

### 5.3 意外优势：证据引用零幻觉（D3 100%）

25/25 条 quote 全部逐字命中合同原文，且 3 次重生成均保持 9/9。法律 AI 最 feared 的"编造合同内容"问题在 hy3-preview 上**未出现**。这与 D4 的"无编造法条"相互印证：**hy3-preview 在证据层面是可信的**，其失分集中在判断层（该不该报风险）而非事实层（引用了什么）。

---

## 6. 改进路线图

| 优先级 | 措施 | 预期收益 | 验证方式 |
|---|---|---|---|
| P0 | prompt 注入异常法条检测指令："如发现法条编号异常（如超出该法条目范围），标注为高风险并提示疑似伪造" | labor_fake_law 从 29.4% → 70%+ | 重跑该样本评测 |
| P0 | unknown 分类降级策略：类型不确定时仍输出"疑似 XX 合同"并完成风险扫描 | 消除整体放弃行为 | 对抗样本回归 |
| P1 | D2 降噪：prompt 约束"仅标注有明确法律依据的风险，合规条款不报" | D2 得分率 33% → 60%+ | 全样本重评 |
| P2 | 简单合同引用具体法条编号（要求给出《XX法》第X条而非一般原则） | D4 从 2/3 → 3/3 | D4 重评 |
| P2 | noncontract 负例补充 reference 判定逻辑 | 负例评测完整 | 规则测试 |
| P3 | 扩充样本集（每场景 ≥10 份，含更多对抗变体） | 评测置信度提升 | 新一轮 T10 |

---

## 7. 成本与复现

| 项目 | hy3-preview 调用 | glm-5.3 调用 | 说明 |
|---|---|---|---|
| 候选审阅生成（T10） | 7 | 0 | 每样本 1 次，已缓存 |
| 一致性重生成（T08） | 2 | 0 | labor_contract × 2 |
| 裁判评分（D4/D6） | 0 | ~10 | 批处理优先+回退，已缓存 |
| 规则维度（D1/D2/D3/D5/D7） | 0 | 0 | 纯本地 |
| **合计** | **9** | **~10** | 重复评测零消耗（缓存命中） |

复现命令：
```bash
python examples/eval_samples.py --all --review-source live   # 完整 7 维（缓存）
python examples/eval_samples.py --all --review-source live --no-judge  # 免费 5 维
python examples/discriminability.py                          # T07 判别力
python examples/consistency.py --with-model                  # T08（复用已存 run 文件则零 token）
```

---

## 8. 结论

1. **评测体系成立**：7 维 rubric 具备判别力（T07 ✓）、确定性（T08 ✓）与可复现性（缓存机制），可以作为合同审阅质量的可靠度量。
2. **产品基本可用但有明确短板**：租赁场景已达到专家水平（100%）；劳动场景受误报和对抗样本失效拖累。修复 P0 两项后，预期整体平均分可从 71.9% 提升至 80%+。
3. **hy3-preview 的法律场景画像**：事实层（条款抽取、证据引用、法条真实性）表现优秀且稳定；判断层（风险边界把握、异常处理策略）是主要提升空间。
4. **方法论价值**：本项目验证了"规则维度免费评 + 主观维度解耦裁判 + 双重缓存"的低成本评测范式，全程 9 次被评模型调用完成 7 样本 × 7 维 × 3 轮一致性 × 判别力对照的全套验证，可直接迁移到其他垂直领域的 LLM 应用评测。

---

*本报告综合 T07/T08/T10 数据自动整理，明细见 `T10_评测报告.md`、`T08_一致性验证.md` 及 `eval/results/` 下各 JSON 结果。*
