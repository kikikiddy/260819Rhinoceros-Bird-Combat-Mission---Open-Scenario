# T14 Roadmap：评估框架向「法规问答」「案件检索与归纳」扩展

- 日期：2026-08-26
- 背景：V1 已聚焦「合同审阅」，其 **7 维 rubric + 规则免费评 + 主观维度解耦裁判 + 双重缓存** 的低成本评测范式已被验证有效（T07/T08/T09/T10/T11）。本 Roadmap 将该范式复用到计划书（M0）中预留的另外两个法律场景。

---

## 1. 复用的核心范式

| 机制 | 说明 | 在扩展场景的复用 |
|---|---|---|
| 规则维度免费评 | D1/D2/D3/D5/D7 纯本地计算 | 可机械化项（引用可追溯、格式、拒答）继续走规则 |
| 主观维度解耦裁判 | 裁判模型 ≠ 被评模型 | 事实准确性、可理解性等仍由独立 judge 评分 |
| 双重缓存 | live 审阅 + judge 结果缓存 | 重复评测零 token，适合大样本 |
| 7 维思想 | 覆盖 抽取/识别/追溯/依据/合规/可理解/格式 | 每个新场景据此裁剪 5–7 个维度 |

---

## 2. 场景二：法规问答（Legal QA）

**任务**：用户就某法条/制度提问，模型给出准确、可溯源、合规的解答。

| 维度 | 关注点 | 评分方式 | 成本 |
|---|---|---|---|
| Q1 事实准确性 | 引用的法条/术语真实、适用，无编造（对应 D4） | 规则正则（可疑编号红标）+ judge/人工核验 | 规则免费 + 少量 judge |
| Q2 引用可追溯 | 给出法条编号/出处，可核查（对应 D3） | 规则：引用与权威库/原文匹配 | 免费 |
| Q3 相关性完整性 | 答其所问，覆盖关键要点（对应 D1/D2） | 规则：关键词召回 + judge | 混合 |
| Q4 安全合规 | 不越界提供执业意见、含免责（对应 D5） | 规则：免责声明 + 拒绝对未授权事项下结论 | 免费 |
| Q5 用户可理解 | 通俗、给可操作指引（对应 D6） | judge | judge token |
| Q6 拒答能力（新增） | 对超出范围/无法核实的问题明确拒答或标注不确定 | 规则：检测"不确定/建议咨询律师"等信号 | 免费 |

> 关键新增 **Q6 拒答能力**：法规问答高危点是"模型自信编造"。需在 prompt 与评测中强制"无法核实须声明不确定"，并以 Q6 度量。

---

## 3. 场景三：案件检索与归纳（Case Retrieval & Summarization）

**任务**：给定争议要点，检索相关判例并归纳裁判要旨。

| 维度 | 关注点 | 评分方式 | 成本 |
|---|---|---|---|
| C1 召回相关性 | 检索结果与问题语义相关、无无关噪声 | 规则：与 query 关键词/标注相关集重合 | 免费 |
| C2 归纳完整性 | 关键要素全覆盖（当事人/争议焦点/裁判要旨/结果） | 规则：对照要素清单命中 | 免费 |
| C3 来源可追溯 | 给出案号/法院/出处，可核查（对应 D3） | 规则：案号格式校验 + 与权威库匹配 | 免费 |
| C4 时效与地域准确性 | 适用最新、正确地域的司法解释 | judge/人工核验（规则难覆盖） | judge token |
| C5 客观无立场 | 归纳不夹带主观倾向、不替当事人下结论 | judge | judge token |
| C6 格式规范 | 结构化输出、字段齐全（对应 D7） | 规则 | 免费 |

> 与合同审阅的映射：C1≈D1、C2≈D2、C3≈D3、C4≈D4、C5≈D6、C6≈D7；**C4 时效地域**是案件场景特有难点，需重点建设 judge 校验或接入法规时效库。

---

## 4. 实施路线（建议顺序）

1. **样本构建（T05 扩展）**：每个新场景 ≥10 份样本，含好/中/差与对抗变体（如法规问答的"诱导编造法条"、案件归纳的"过期判例"）。
2. **rubric 落地**：复用 `eval/rubric.py` 的 `Dim`/`score_all` 结构，新增 `Q_DIMENSIONS` / `C_DIMENSIONS` 两个维度表，规则函数照猫画虎实现。
3. **管线接入**：`examples/eval_samples.py` 增加 `--task qa|case` 开关，复用评分与缓存逻辑；裁判注入点不变。
4. **元验证**：对每个新场景重跑判别力（好/坏档对照）与一致性（多轮重跑），确认 rubric 可靠后再做完整评测。
5. **迭代**：沿用 T11 的"失败模式归因 → 改进路线图"闭环。

---

## 5. 复用价值

合同审阅验证过的"**规则维度零成本 + 主观维度解耦裁判 + 双重缓存**"范式，可直接迁移到法规问答与案件检索，使三个法律场景共享同一套评测基础设施，整体评测成本仍维持低位（被评模型调用 ≈ 样本数 × 轮次，裁判维度经缓存与批处理进一步压缩）。
