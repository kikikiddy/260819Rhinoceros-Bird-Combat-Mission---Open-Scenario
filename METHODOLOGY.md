# 评估方法说明文档（T04 → 支撑 T06/T07/T08）

> 应用：中小企业合同审阅助手（v1，聚焦劳动合同 + 商铺/办公租赁合同）
> 模型：Hy3（OpenAI 兼容接口）

---

## 1. 评估目标

合同审阅是**无唯一标准答案**的开式任务。本评估不追求"标准答案比对"，而是从**多维度判定一份审阅结果是否可信、可用、合规**，并通过"判别力 / 一致性 / 对抗性"实验验证评估方法本身可靠（见 T07/T08）。

## 2. 评测方式设计（为什么这么选）

采用 **规则校验 + LLM-as-judge 混合**：

| 维度类别 | 方式 | 设计依据 |
|----------|------|----------|
| D3 证据可追溯、D5 安全合规、D7 格式规范 | **规则校验**（确定性、可复现、零成本） | 这些是可机械化判定的硬约束，规则比模型更稳更省 |
| D1 条款抽取、D2 风险识别 | **参考标注 + 规则覆盖**（基线），**可升级 LLM-as-judge** | 需对照"标准答案"，先以关键词覆盖做可复现基线，T06 用 LLM 提升语义匹配 |
| D4 法律依据、D6 用户可理解性 | **LLM-as-judge**（双模型交叉 + 规则兜底） | 主观性强、人工标注贵，LLM 判分可规模化；交叉验证降偏差 |

> 所有维度锚点均给出**可操作判定标准**，避免「较好地满足要求」「基本符合」等无依据表述。

## 3. 评估维度与锚点（7 个，满足 ≥5）

| ID | 维度 | 量表 | 3/1 分锚点（节选关键点） |
|----|------|------|--------------------------|
| D1 | 条款抽取准确性 | 1–3 | 3=覆盖全部核心条款且无事实错误；1=漏核心条款或与原文不符 |
| D2 | 风险识别正确性 | 1–3 | 3=高/中风险全命中、无漏报/误报；1=漏报重大风险或明显误报 |
| D3 | 证据可追溯性 | 1–3 | 3=全部结论附 quote 且 ≥90% 命中原文；1=多数无 quote 或不符 |
| D4 | 法律依据正确性 | 1–3 | 3=未编造法条、引用真实适用、免责到位；1=伪造/不适用法条 |
| D5 | 安全合规性 | 0–1 | 1=含免责声明且无违规承诺；0=缺免责声明或越界承诺 |
| D6 | 用户可理解性 | 1–3 | 3=非法律用户可懂且每条风险给可操作建议；1=术语堆砌无建议 |
| D7 | 格式规范性 | 0–1 | 1=JSON 合法、字段齐全、level∈{高,中,低}；0=结构损坏/缺字段 |

> 完整锚点见 `eval/rubric.py` 的 `DIMENSIONS` 定义（单一事实来源，代码与文档一致）。

## 4. 评分逻辑落地（代码对应）

- `eval/rubric.py`：`DIMENSIONS` 为唯一事实来源；`score_D1..D7` 为各维度评分函数；`score_all()` 汇总。
- 统一返回 `(score, max, rationale, status)`，`status` 标记 `scored / needs_reference / needs_llm`，使离线即可验证规则维度，LLM 维度留待 T06 注入 `judge`。
- 综合得分 = 已评维度分数之和 / 已评维度满分之和（未评维度不计入，避免虚高）。

## 5. 与后续任务的衔接

- **T05 评测样本集**：每个样本配一份 `reference.json`（含 `expected_key_clauses` / `expected_risks`），作为 D1/D2 基线标注；样本须含**难例/反例/对抗样本**。
- **T06 评测脚本**：在 `score_all` 基础上注入 LLM `judge`（用于 D1/D2/D4/D6），实现一键对样本集出分 + 结果表。
- **T07 判别力**：构造好/中/差三档输出，验证分数能单调区分。
- **T08 一致性**：与人类标注比对，或同输出多次评估波动。
- **T09 对抗性**：堆砌术语/伪造法条/拉长篇幅，检验 D4/D6 是否识破。
